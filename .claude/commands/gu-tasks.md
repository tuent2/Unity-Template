---
description: Chẻ design/feature thành task 1-4h có dependency và test note
argument-hint: [feature / design / brief]
---

Dùng skill `unity-design-to-tasks` cho: **$ARGUMENTS**

Yêu cầu:
- Mỗi task 1–4 giờ, có `Why`, `Files dự kiến`, `Core API tái dùng`, `Definition of done`, `Test note`, `Depends on`.
- Đánh dấu task chạy song song được và task đụng scene/prefab (tách khỏi task code).
- Thêm ít nhất một task giảm rủi ro nếu chạm hệ thống mới.
