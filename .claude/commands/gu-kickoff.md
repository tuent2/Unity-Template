---
description: Chốt scope + acceptance criteria cho một feature Unity trước khi code
argument-hint: [tên feature / mô tả ngắn]
---

Dùng skill `unity-feature-kickoff` cho feature: **$ARGUMENTS**

Yêu cầu:
- Xác định player value, in scope, out of scope.
- Grep `Assets/TuentCore/Runtime` và `Assets/_MainProject` để liệt kê hệ thống có sẵn nên tái dùng.
- Acceptance criteria phải đo được.
- Hỏi lại nếu thiếu: platform đích, thiết bị thấp nhất, ngân sách hiệu năng.

Chưa viết code ở bước này.
